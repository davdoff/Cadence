{
  "name": "cadence-server",
  "version": "1.0.0",
  "description": "Cadence planning backend: /v1 typed planning API + legacy /api proxy",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "start:legacy": "node server.js",
    "test": "node --test \"server/test/*.test.js\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "luxon": "^3.7.2",
    "node-ical": "^0.26.1",
    "rrule": "^2.8.1"
  }
}
